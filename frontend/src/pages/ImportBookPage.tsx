import { Box } from "@mui/material";
import type { ReactElement } from "react";

const ImportBookPage = (): ReactElement => (
  <Box padding="24px">Not Implemented</Box>
);

export default ImportBookPage;
